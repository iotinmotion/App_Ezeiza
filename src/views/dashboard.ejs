<!DOCTYPE html>
<html lang="es">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dashboard - Disponibilidad</title>
  <link rel="stylesheet" href="/css/style.css">
  <style>
    .filter-buttons {
      margin-bottom: 15px;
      display: flex;
      gap: 10px;
    }
    .btn-filter {
      padding: 5px 12px;
      border: 1px solid #ddd;
      background: #f8f9fa;
      border-radius: 20px;
      cursor: pointer;
      font-size: 0.85em;
      transition: all 0.2s;
    }
    .btn-filter:hover {
      background: #e2e6ea;
    }
    .btn-filter.active {
      background: #007bff;
      color: white;
      border-color: #007bff;
    }
    /* Grid responsive para sensores: se adapta al ancho disponible */
    .sensor-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(80px, 1fr));
      gap: 10px;
    }
    /* Animaciones para sensores */
    .card-transition {
      transition: opacity 0.3s ease, transform 0.3s ease;
      opacity: 1;
      transform: scale(1);
    }
    .card-transition.hiding {
      opacity: 0;
      transform: scale(0.95);
      pointer-events: none;
    }
    .card-transition.hidden {
      display: none;
    }
  </style>
</head>

<body>

  <%- include('partials/header', { page: 'dashboard', user: user }) %>

  <!-- Contenido Principal -->
  <main class="dashboard-content">
    <div class="container">
      <h2>🛒 Control de Carritos</h2>

      <!-- Elemento oculto para preservar data-app-id requerido por JS -->
      <div id="summaryGrid" data-app-id="<%= user.app %>" style="display: none;"></div>

      <!-- Tarjeta de Referencias -->
      <div class="card" style="margin-top: 20px;">
        <div class="card-body legend-content">
          <div class="legend-item legend-filter" data-filter="critico" style="cursor: pointer; transition: opacity 0.3s;">
            <span class="status-dot" style="background-color: #dc3545;"></span>
            <span>Critico (0-20%)</span>
          </div>
          <div class="legend-item legend-filter" data-filter="medio" style="cursor: pointer; transition: opacity 0.3s;">
            <span class="status-dot" style="background-color: #fd7e14;"></span>
            <span>Medio (20-80%)</span>
          </div>
          <div class="legend-item legend-filter" data-filter="completo" style="cursor: pointer; transition: opacity 0.3s;">
            <span class="status-dot" style="background-color: #28a745;"></span>
            <span>Normal (80-100%)</span>
          </div>
        </div>
      </div>

      <!-- Sección Dividida: Detalles y Extra -->
      <div class="card" style="margin-top: 20px;">
        <div class="card-body">
          <div style="display: flex; align-items: center; justify-content: space-between; margin-bottom: 1rem;">
            <h2 style="margin: 0;">Nivel de Llenado por Zona</h2>
            <span id="connectionStatus" class="offline-badge">Sin conexión</span>
          </div>
          <div class="inner-card-bg">
            <div class="sensor-grid" id="sensorGrid">
              <% if (typeof cartDetails !=='undefined' && cartDetails) { %>
                <% 
                  const colorPriority = { '#dc3545': 1, '#fd7e14': 2, '#28a745': 3 };
                  const getPriority = (c) => colorPriority[c] || 4;
                  cartDetails.sort((a, b) => getPriority(a.color) - getPriority(b.color));
                %>
                <% cartDetails.forEach(cart=> { %>
                  <% const pct = cart.percentage !== undefined ? cart.percentage : Math.round((cart.cart_counter / (cart.cart_counter_max || 1)) * 100); %>
                  <!-- Tooltip personalizado usando data-tooltip -->
                  <div class="sensor-square card-transition" data-tooltip="Carritos: <%= cart.cart_counter %>"
                    style="background-color: <%= cart.color || '#999' %>;"
                    data-sensor-zone="<%= cart.zone || 'Zona' %>"
                    data-color="<%= cart.color || '#999' %>"
                    data-percentage="<%= pct %>">
                    <div style="font-size: 0.7em; margin-bottom: 2px;">
                      <%= cart.zone || 'Zona' %>
                    </div>
                    <div>
                      <%= pct %>%
                    </div>
                  </div>
                  <% }) %>
                    <% } %>
            </div>
          </div>
        </div>
      </div>

    </div>
  </main>

  <%- include('partials/footer') %>

  <script src="/socket.io/socket.io.js"></script>
  <script src="/js/script.js"></script>
</body>

</html>