<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - App Ezeiza</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="login-body">
    
    <div class="login-container">
        <div class="login-header">
            <img src="/img/logo.jpg" alt="App Ezeiza" class="logo-img" style="width: 100%; height: auto; display: block;">
            <p>Gestión de Carritos</p>
        </div>

        <% if (error) { %>
            <div class="alert-error"><%= error %></div>
        <% } %>

        <form action="/login" method="POST" id="loginForm">
            <div class="form-group">
                <label for="username">Usuario</label>
                <input type="text" id="username" name="username" placeholder="Ingrese su usuario">
                <small class="error-msg" id="userError"></small>
            </div>

            <div class="form-group">
                <label for="password">Contraseña</label>
                <input type="password" id="password" name="password" placeholder="Ingrese su contraseña">
                <small class="error-msg" id="passError"></small>
            </div>

            <button type="submit" class="btn-primary">Ingresar</button>
        </form>
        
        <div class="theme-toggle-container">
            <button id="themeToggle" class="btn-text">Cambiar Tema 🌓</button>
        </div>
    </div>

    <script src="/js/script.js"></script>
</body>
</html>