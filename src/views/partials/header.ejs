  <!-- Header / Navegación -->
  <header class="main-header">
    <div class="container header-content">
      <div class="logo-area">
        <!-- Optimización: width/height definidos para evitar saltos de carga -->
        <img src="/img/logo.jpg" alt="App Ezeiza" class="logo-circle" width="40" height="40">
        <span class="logo-text">Aeropuerto Ezeiza</span>
      </div>

      <nav class="main-nav">
        <a href="/dashboard" class="<%= (typeof page !== 'undefined' && page === 'dashboard') ? 'active' : '' %>">Dashboard</a>
        <a href="/dashboard/info" class="<%= (typeof page !== 'undefined' && page === 'info') ? 'active' : '' %>">Información Adicional</a>
        <a href="/dashboard/maps" class="<%= (typeof page !== 'undefined' && page === 'maps') ? 'active' : '' %>">Mapas</a>
        <a href="#">Reportes</a>
      </nav>

      <div class="user-area">
        <button id="themeToggle" class="btn-icon">🌓</button>
        <span class="username">
          <%= user.username %> (<%= user.role %>)
        </span>
        <img src="/img/logo.jpg" alt="User" class="user-photo">
        <a href="/logout" class="btn-logout">Salir</a>
      </div>
    </div>
  </header>